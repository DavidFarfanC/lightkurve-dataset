{
  "version": 2,
  "functions": {
    "api/index.py": {
      "runtime": "python3.11",
      "buildCommand": "pip install -r requirements.txt",
      "maxDuration": 60
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "api/index.py" }
  ]
}
